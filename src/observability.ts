import { randomUUID } from 'node:crypto';
export interface TraceEvent { traceId:string; spanId:string; parentSpanId?:string; name:string; at:string; durationMs?:number; status:'started'|'ok'|'error'; detail?:Record<string,unknown>; }
export class TraceRecorder { readonly events:TraceEvent[]=[]; start(name:string,traceId=randomUUID(),parentSpanId?:string){ const spanId=randomUUID(),started=Date.now();this.events.push({traceId,spanId,parentSpanId,name,at:new Date(started).toISOString(),status:'started'});return{traceId,spanId,end:(detail?:Record<string,unknown>)=>this.events.push({traceId,spanId,parentSpanId,name,at:new Date().toISOString(),durationMs:Date.now()-started,status:'ok',detail}),fail:(error:unknown)=>this.events.push({traceId,spanId,parentSpanId,name,at:new Date().toISOString(),durationMs:Date.now()-started,status:'error',detail:{error:error instanceof Error?error.message:String(error)}})};} }
