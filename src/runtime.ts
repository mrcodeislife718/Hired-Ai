import type { CandidateProfile, Evidence } from './domain.js';
import { HiredEngine } from './engine.js'; import type { PersistenceAdapter } from './persistence.js'; import { persistenceFromEnv } from './persistence.js'; import { TraceRecorder } from './observability.js';
export class HiredRuntime { readonly traces=new TraceRecorder();private saveTimer?:NodeJS.Timeout;private constructor(readonly engine:HiredEngine,readonly persistence:PersistenceAdapter){}static async create(profile:CandidateProfile,seedEvidence:Evidence[]=[],persistence=persistenceFromEnv()){const snapshot=await persistence.load();const engine=new HiredEngine(profile,snapshot?.evidence.length?snapshot.evidence:seedEvidence);if(snapshot)engine.store.restore(snapshot);return new HiredRuntime(engine,persistence);}async checkpoint(){const span=this.traces.start('persistence.checkpoint');try{await this.persistence.save(this.engine.store.snapshot());span.end({opportunities:this.engine.store.opportunities.size});}catch(error){span.fail(error);throw error;}}startAutoCheckpoint(intervalMs=Number(process.env.HIRED_CHECKPOINT_MS??5000)){this.stopAutoCheckpoint();this.saveTimer=setInterval(()=>void this.checkpoint(),intervalMs);this.saveTimer.unref();}stopAutoCheckpoint(){if(this.saveTimer)clearInterval(this.saveTimer);this.saveTimer=undefined;}async close(){this.stopAutoCheckpoint();await this.checkpoint();await this.persistence.close?.();}}
