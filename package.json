{
  "name": "hired-ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Governed, evidence-grounded autonomous job acquisition agent",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "npm run build && npm test",
    "test": "node --test dist/test/*.test.js",
    "demo": "npm run build && node dist/src/cli.js demo",
    "serve": "npm run build && node dist/src/server.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.2"
  },
  "engines": { "node": ">=22" }
}
